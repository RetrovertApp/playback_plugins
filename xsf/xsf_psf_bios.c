///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Embedded PSX BIOS for PSF playback via highly_experimental
//
// This file wraps a pre-generated HE BIOS (SCPH-10000 processed through mkhebios)
// as a static C array. The data is ~512KB and contains the minimal IOP modules
// needed by highly_experimental to play PSF and PSF2 files.
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#include "xsf_psf_bios.h"

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

static const uint32_t s_he_bios_data[] = {
#include "scph10000_he.inc"
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

const uint8_t* xsf_psf_get_embedded_bios(uint32_t* out_size) {
    if (out_size != ((void*)0)) {
        *out_size = (uint32_t)sizeof(s_he_bios_data);
    }
    return (const uint8_t*)s_he_bios_data;
}
